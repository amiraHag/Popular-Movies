<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/item_margin"
    android:background="@color/movie_card_bg"
    android:orientation="vertical"
    android:padding="@dimen/inline_text_margin">

    <ImageView
        android:id="@+id/iv_detailed_movie_poster"
        android:layout_width="match_parent"
        android:layout_height="@dimen/movie_poster_height"
        android:scaleType="fitXY" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_movie_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="4"
            android:paddingRight="@dimen/first_title_padding"
            android:textColor="@color/first_title"
            android:textSize="@dimen/first_title_text_size" />

        <TextView
            android:id="@+id/tv_user_rate"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/red"
            android:textSize="@dimen/user_rate_text_size" />
    </LinearLayout>

</LinearLayout>
